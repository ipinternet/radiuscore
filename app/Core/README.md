# Core

The Core namespace contains reusable platform abstractions and building blocks.

Core must remain independent of business domains.

Allowed dependencies:

- PHP
- Laravel framework components (when appropriate)

Forbidden dependencies:

- Modules
- Filament Resources
- Business logic